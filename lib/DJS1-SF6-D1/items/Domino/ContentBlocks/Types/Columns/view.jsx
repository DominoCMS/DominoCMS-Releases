DominoViews.registerView( 'Domino.ContentBlocks.Types.Columns', function( data ) {	"use strict";    var cls = data.class ? data.class : '';    var theme = data.theme ? 'domino-contentblocks ' + data.theme + ' ' + cls : 'domino-contentblocks white' + ' ' + cls;    return <div class={ theme }>        <div class="grid-container">        { (function () {            var ret = [];            if ( data.name )                ret.push(<div class="grid-x grid-padding-x">                    <div class="small-12 cell text-center small-padding-bottom-2">                        <h2>{ data.name }</h2>                        { (function () {                            var ret2 = [];                            if ( data.subtitle )                                ret2.push(<h3>{ data.subtitle }</h3>);                            return ret2;                        })() }                    </div>                </div>);            return ret;        })() }			<div class="grid-x grid-padding-x">				{ (function () {					if ( data.children ) {						var ret = [];                        if ( data.children.length == 4 )                            var componentClass = 'small-12 medium-6 large-3 cell';                        else if ( data.children.length == 3 )                            var componentClass = 'small-12 medium-4 large-4 cell';                        else if ( data.children.length == 2 )                            var componentClass = 'small-12 medium-6 large-6 cell';                        else if ( data.children.length == 1 )                            var componentClass = 'small-12 medium-12 large-12 cell';						for (var i = 0; i < data.children.length; i++) {							var block = data.children[i];                            if ( block.class )                                var componentClass = block.class;                            if ( block.component )                                ret.push(<div class={ componentClass }>                                        { (function () {                                            var ret2 = [];                                            if ( block.name )                                                ret2.push(<h3>{ block.name }</h3>);                                            if ( block.subtitle )                                                ret2.push(<h4>{ block.subtitle }</h4>);                                            if ( block.content )                                                ret2.push( DCUtil.displayHtml( block.content ) );                                            return ret2;                                    })() }                                    <component view={ block.component } componentData={ block.componentData }/>                                </div>);                            else							    ret.push(<div class={ componentClass }>                                        { (function () {                                            var ret2 = [];                                            if ( block.picProfile )                                                    ret2.push(<img src={ block.picProfile.filename } class="small-padding-bottom-1" />);                                            if ( block.name )                                                ret2.push(<h3>{ block.name }</h3>);                                            if ( block.subtitle )                                                ret2.push(<h4>{ block.subtitle }</h4>);                                            return ret2;                                        })() }                                    { DCUtil.displayHtml(block.content ) }                                </div>);						}						return ret;					}				})() }		</div>            </div>	</div>;});