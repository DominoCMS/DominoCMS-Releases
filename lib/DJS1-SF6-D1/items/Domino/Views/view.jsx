DominoViews.registerView( 'Domino.Views', function( data ) {	"use strict";	return <div>{ (function () {		if ( data.length ) {			var ret = [];			for (var i = 0; i < data.length; i++) {				var view = data[i];				if ( view.component == 'inner')					ret.push(<div id="inner"></div>);				else {                    if ( view.componentData )                        ret.push(<component view={ view.developer + '.' + view.component } componentData={ view.componentData } />);					else                        ret.push(<component view={ view.developer + '.' + view.component } />);				}			}			return ret;		}	})() }</div>;});