DominoViews.registerView( 'Domino.Views', function( data ) {	"use strict";	return {tag: "div", attrs: {}, children: [ (function () {		if ( data.length ) {			var ret = [];			for (var i = 0; i < data.length; i++) {				var view = data[i];				if ( view.component == 'inner')					ret.push({tag: "div", attrs: {id:"inner"}});				else {                    if ( view.componentData )                        ret.push(br("component", {view: view.developer + '.' + view.component,componentData: view.componentData}));					else                        ret.push(br("component", {view: view.developer + '.' + view.component}));				}			}			return ret;		}	})() ]};});
