DominoViews.registerView( 'Domino.Admin.Header.Account', function( data ) {	"use strict";	if ( data.auth === true )		return <div>					<nav class="header-button right">						<dt>							<a class="DominoTooltip" id="ButtonAccount" href="#" title="Uporabniški meni">								{ (function () {									if ( data.pic )										return <span class="image hide-for-small-only" style={ 'background-image:url(' + pic + ');' }></span>;										else										return <span class="image hide-for-small-only"></span>								})() }							  <span class="name hide-for-small-only">{ data.auth_name } { data.auth_surname }</span>							  <span class="icon-login"></span>							</a>						</dt>						<dd class="rt" id="account-list">							<ul>							{ (function () {								if ( data.entries ) {										var ret = [];										for ( var e = 0; e < data.entries.length; e++ ) {											var menu = data.entries[ e ];											ret.push( <li>												<a href={ menu.link }><span class="icon-{ menu.icon }"></span>{ menu.name }</a>											</li> );										}										return ret;								}							})() }							</ul>						</dd>					</nav>				</div>} );