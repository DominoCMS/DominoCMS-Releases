DominoViews.registerView( 'Domino.Admin.Entry.Selector', function( data ) {	"use strict";	var id = data.id ? data.id : '';    var icon = data.icon ? data.icon : 'icon-arrow_up';	return <div class="domino-miniselector" id={ id }>		<dt>			<a href="#" class="add-button">				<span class={ icon }></span>			</a>            { (function () {                var ret = [];                if ( data.form ) {                    var type = data.form.type ? data.form.type : 'hidden';                    ret.push(<input type={ type } value={ data.form.value } name={ data.form.element } class="mini-selector-input" />);                    ret.push(<div class="sel">                        <div class="select" id={ 'span' + data.form.element }>{ data.form.name }</div>                    </div>);				}                return ret;            })() }		</dt>		<dd name="miniSelector" id={ 'miniSel' + id }></dd>	</div>} );