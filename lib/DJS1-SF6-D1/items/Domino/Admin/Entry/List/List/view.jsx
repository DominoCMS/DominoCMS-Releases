DominoViews.registerView( 'Domino.Admin.Entry.List.List', function( data ) {	"use strict";	return <table>		<thead>		<tr>			{ (function () {				var ret = [];				ret.push( <th class="tdArrow"></th> );				for ( var i = 0; i < data.columns.length; i++ ) {					var column = data.columns[i],						width = column.width ? 'width:' + column.width : '';					if ( ( column.structureType == 'order' ) || ( column.structureType == 'bool' ) || ( column.structureType == 'delete' ) || ( column.structureType == 'edit' ) || ( column.structureType == 'deploy' ) )						width = 'width:5%';					// add sortable					ret.push( <th style={ width }>{ column.name }</th> );				}				return ret;			})() }				</tr>			</thead>		<component view="Domino.Admin.Entry.List.List.Entries" componentData={ data } />	</table>} );