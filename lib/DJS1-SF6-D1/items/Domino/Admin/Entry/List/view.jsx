DominoViews.registerView( 'Domino.Admin.Entry.List', function( data ) {	"use strict";	return <div class="domino-entry-list">        { (function () {            var ret = [];            if ( data.actions )                ret.push(<component view="Domino.Admin.Entry.List.Actions" componentData={ data.actions } />);            if ( data.subactions )                ret.push(<component view="Domino.Admin.Entry.List.SubActions" componentData={ data.subactions } />);            return ret;        })() }		<component view="Domino.Admin.Entry.List.List" componentData={ data.list } />	</div>});