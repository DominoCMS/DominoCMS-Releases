DominoViews.registerView( 'Domino.Admin.Entry.List.SubActions', function( data ) {	"use strict";	if ( data )		if ( data.buttons )		return <div class="domino-subactions grid-x grid-padding-x">						{ (function () {							if ( data.buttons ) {								var ret = [];								for (var key in data.buttons) {									// skip loop if the property is from prototype									if (!data.buttons.hasOwnProperty(key)) continue;									var entry = data.buttons[key];									if ( key == 'filterEntries')										ret.push(<div class="pad shrink cell">											<input type="text" name="FilterEntries" autocomplete="off" placeholder="Filtriraj ..." class="filterEntries" />										</div>);									else if ( key == 'langSwitcher')										ret.push(<div class={ 'pad shrink cell ' + entry.position }>												{ (function () {													var ret2 = [];													if ( data.dimensions.length )														ret2.push(<span class="text">Select language</span>);													else {                                                        ret2.push(<span class="text">Language</span>);                                                        ret2.push(<a href="#" lang={ data.lang } class="is-active" disabled>{ data.lang }</a>);													}													for ( var i = 0; i < data.dimensions.length; i++ ) {														var dimension = data.dimensions[i];														var buttonClass = ( dimension.id == data.lang ) ? 'is-active' : '';														ret2.push(<a lang={ dimension.id } name="langSwitcher" href="#" class={ buttonClass }>{ dimension.name }</a>);													}													return ret2;												})() }										</div>);								}								return ret;							}						})() }				</div>} );