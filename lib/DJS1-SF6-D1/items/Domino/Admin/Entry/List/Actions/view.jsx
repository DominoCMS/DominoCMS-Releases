DominoViews.registerView( 'Domino.Admin.Entry.List.Actions', function( data ) {	"use strict";	if ( data )		return <div class="grid-x domino-actions">						{ (function () {							if ( data.buttons ) {								var ret = [];								for (var key in data.buttons) {									// skip loop if the property is from prototype									if (!data.buttons.hasOwnProperty(key)) continue;									var entry = data.buttons[key];									ret.push(<div class={ 'shrink cell ' + entry.position }>										<a href={ entry.link } id={ key + 'Action' } title={ entry.value }>											<span class={ 'icon-' + entry.icon }></span>											<span class="hide-for-small-only">{ entry.value }</span>										</a>									</div>);								}								return ret;							}						})() }				</div>} );