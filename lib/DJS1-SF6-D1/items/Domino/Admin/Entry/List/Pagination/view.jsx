DominoViews.registerView( 'Domino.Admin.Entry.List.Pagination', function( data ) {	"use strict";    if ( data.translate )        return <div class="domino-pagination grid-x">                    <div class="small-12 medium-6 cell">                        { '' + data.translate.page } <span class="pagination-current">{ '' + data.currentPage }</span> { data.translate.pageOf } <span class="pagination-all">{ '' + data.pages }</span> ({ data.translate.allEntries }: <span class="pagination-entries">{ '' + data.numEntries }</span>)                    </div>                     <div class="small-12 medium-6 cell text-right">                        <a href="" name="pagePrev" class={ data.currentPage == 1 ? 'paginate active' : 'paginate' }>                            <span class="icon-arrow_left"></span>                        </a>                         { (function () {                             var ret = [];                             for ( var i = 0; i < data.pages; i++ ) {                                 var page = i + 1;                                 var active = ( page == data.currentPage ) ? 'paginate active' : 'paginate';                                 // add sortable                                 ret.push( <a href={ '' + page } name={ '' + page } class={ active }>{ '' + page }</a> );                             }                             return ret;                         })() }                        <a href="" name="pageNext" class={ data.currentPage == data.pages ? 'paginate active' : 'paginate' }>                            <span class="icon-arrow_right"></span>                        </a>                    </div>        </div>} );