DominoViews.registerView( 'Domino.Admin.Entry.Edit.Field.TabSet', function( data ) {	"use strict";	var ret = [];	ret.push(<div class="domino-tabs row">		<ul>			{ (function () {				var ret2 = [];				for ( var i = 0; i < data.children.length; i++ ) {					var str = data.children[i],						active = ( i === 0 ) ? 'is-active' : '';					ret2.push(<li class={ active } data-id={ 'tab' + str.developer + str.module + str.id }>						<a href="#">{ str.name }</a>					</li>);				}				return ret2;			})() }		</ul>	</div>);	ret.push(<div class="domino-tabs-content">		{ (function () {			var ret2 = [];			for ( var i = 0; i < data.children.length; i++ ) {				var str = data.children[i],					active = ( i === 0 ) ? 'tabs-panel is-active' : 'tabs-panel';				ret2.push(<div data-id={ 'tab' + str.developer + str.module + str.id } class={ active }>					{ (function () {						var ret3 = [];						for (var j = 0; j < str.children.length; j++) {							var componentData = str.children[j];							componentData['dimensions'] = data.dimensions;							ret3.push(<component view={ 'Domino.Admin.Entry.Edit.Field.' + componentData.elementItem } componentData={ componentData } />);						}						return ret3;					})() }				</div>);			}			return ret2;		})() }	</div>);	return <div class="domino-tabset">{ ret } </div>;});