DominoViews.registerView( 'Domino.Admin.Entry.Edit.Field.FieldSet', function( data ) {	"use strict";	return <fieldset class="domino-fieldset">		{ (function () {			var ret = [];            if ( data.structureName )                ret.push(<legend>{ data.structureName }</legend>);			if ( data.name )				ret.push(<legend>{ data.name }</legend>);			for (var i = 0; i < data.children.length; i++) {				var componentData = data.children[i];				componentData['dimensions'] = data.dimensions;				ret.push(<component view={ 'Domino.Admin.Entry.Edit.Field.' + componentData.elementItem } componentData={ componentData } />);			}			return ret;		})() }	</fieldset>});