DominoViews.registerView( 'Domino.Admin.Entry.Edit', function( data ) {	"use strict";	return <div class="domino-entryedit">        <form id="editForm" method="post" enctype="multipart/form-data">            { (function () {                var ret = [];                if ( data.actions )                    ret.push(<component view="Domino.Admin.Entry.List.Actions" componentData={ data.actions } />);                if ( data.subactions )                    ret.push(<component view="Domino.Admin.Entry.List.SubActions" componentData={ data.subactions } />);                return ret;            })() }            <component view="Domino.Admin.Entry.Edit.Title" componentData={ data.title } />            <div class="domino-entryedit-body">            { (function () {                var ret = [];                for (var i = 0; i < data.structure.length; i++) {                    var componentData = data.structure[i];                    componentData['dimensions'] = data.dimensions;                    ret.push(<component view={ 'Domino.Admin.Entry.Edit.Field.' + componentData.elementItem } componentData={ componentData } />);                }                return ret;ss            })() }            </div>        </form>    </div>});